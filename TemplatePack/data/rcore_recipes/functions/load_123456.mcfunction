# Desc: Display in-game text if they are missing the API, RP Must be installed!
#
# Called by: #minecraft:load

# load file, will show text unless API pack is installed, Do not change text!
tellraw @p {"translate":"Missing datapack! - LPS Rcore API","color":"red"}